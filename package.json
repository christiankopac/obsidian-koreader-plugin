{
	"name": "obsidian-koreader-sync",
	"version": "1.0.0",
	"description": "Sync highlights and bookmarks from KOReader to Obsidian",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"version": "node version-bump.mjs && pnpm run build && git add manifest.json versions.json main.js",
		"release": "pnpm run build && zip -r koreader-sync-plugin.zip main.js manifest.json"
	},
	"keywords": [
		"obsidian",
		"koreader",
		"sync",
		"highlights",
		"bookmarks"
	],
	"author": "",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^16.11.6",
		"@typescript-eslint/eslint-plugin": "5.29.0",
		"@typescript-eslint/parser": "5.29.0",
		"builtin-modules": "3.3.0",
		"esbuild": "0.17.3",
		"obsidian": "latest",
		"tslib": "2.4.0",
		"typescript": "4.7.4"
	},
	"dependencies": {
		"gray-matter": "^4.0.3"
	}
}
